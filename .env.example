# 앱 설정
APP_NAME=장독대
DEBUG=False

# 데이터베이스
DATABASE_URL=postgresql://user:password@localhost/jangdokdae

# OAuth
OAUTH_KAKAO_CLIENT_ID=your_kakao_client_id
OAUTH_KAKAO_CLIENT_SECRET=your_kakao_client_secret
OAUTH_GOOGLE_CLIENT_ID=your_google_client_id
OAUTH_GOOGLE_CLIENT_SECRET=your_google_client_secret

# JWT
SECRET_KEY=your_secret_key_here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Gemini Vertex AI (Google Cloud 크레딧 적용)
# vertex ai 인증
GOOGLE_APPLICATION_CREDENTIALS=app/credentials/vertex_key.json
# vertex ai 설정
GOOGLE_CLOUD_PROJECT=kt-cloud-jangdokdae
GOOGLE_CLOUD_LOCATION=asia-northeast3
VERTEX_MODEL=gemini-2.5-flash

# 뉴스 수집
OPENDART_API_KEY=
ECOS_API_KEY=

# Embedding
# 모델 미확정 — 후보(gemini-embedding-001 / nlpai-lab/KURE-v1 / ko-sroberta baseline) 비교 후 확정
EMBED_MODEL=jhgan/ko-sroberta-multitask
EMBED_DIM=768  # 모델에 따라 768(gemini 절단·ko-sroberta) 또는 1024(KURE·bge-m3)
EMBED_BATCH_SIZE=50  # Vertex AI 최대 허용 배치 크기 (설계 05 §2.3)

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:8000
